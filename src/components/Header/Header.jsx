import React from "react";
import { HeaderContainer, HeaderMenu } from "./Header.styled";
const Header = () => {
  return (
    <HeaderContainer>
      <HeaderMenu>
        <h2>Hello Header</h2>
      </HeaderMenu>
    </HeaderContainer>
  );
};

export default Header;
